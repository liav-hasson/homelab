apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: monitoring
spec:
  hostnames:
    - "grafana.quizlabs.org"
  parentRefs:
    - name: quiz-gateway
      namespace: platform
  rules:
    - backendRefs:
        - name: kube-prometheus-stack-grafana
          port: 80
