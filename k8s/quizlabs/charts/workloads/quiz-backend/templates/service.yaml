apiVersion: v1
kind: Service
metadata:
  name: {{ include "quiz-backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "quiz-backend.labels" . | nindent 4 }}

spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quiz-backend.selectorLabels" . | nindent 4 }}