{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "quiz-multiplayer.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "quiz-multiplayer.labels" . | nindent 4 }}
{{- end }}
