apiVersion: v1
kind: Service
metadata:
  name: {{ include "quiz-frontend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "quiz-frontend.labels" . | nindent 4 }}

spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quiz-frontend.selectorLabels" . | nindent 4 }}
